#pragma once
class LightManager
{
public:
	LightManager(void);
	~LightManager(void);

	/*Todo... Directional light.. things in update. Just send in camera and calculate lightDir and things. Then maybe a DOD container for point lights / point light initializer*/
};

