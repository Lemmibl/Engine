struct VertexShaderOutput
{
	float4 Position : SV_POSITION;
	float4 ScreenPosition : TEXCOORD0;
};

void DepthOnlyPixelShader(VertexShaderOutput input)
{
	//float depth = input.ScreenPosition.z / input.ScreenPosition.w;
	//return depth;
}